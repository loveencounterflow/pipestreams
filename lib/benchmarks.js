// Generated by CoffeeScript 2.0.0-beta3
(function() {
  'use strict';
  var $, $async, $split, $stringify, $utf8, CND, FS, O, PATH, PS, STPS, async_map, badge, debug, echo, format_float, format_integer, help, info, new_file_source, new_numeral, pull, rpr, simple_recursive_function, through, urge, warn, whisper;

  CND = require('cnd');

  rpr = CND.rpr;

  badge = 'BASIC-STREAM-BENCHMARKS-2/BENCHMARKS';

  debug = CND.get_logger('debug', badge);

  warn = CND.get_logger('warn', badge);

  info = CND.get_logger('info', badge);

  urge = CND.get_logger('urge', badge);

  help = CND.get_logger('help', badge);

  whisper = CND.get_logger('whisper', badge);

  echo = CND.echo.bind(CND);

  new_numeral = require('numeral');

  format_float = function(x) {
    return (new_numeral(x)).format('0,0.000');
  };

  format_integer = function(x) {
    return (new_numeral(x)).format('0,0');
  };

  PATH = require('path');

  FS = require('fs');

  $split = require('pull-split');

  $stringify = require('pull-stringify');

  $utf8 = require('pull-utf8-decoder');

  new_file_source = require('pull-file');

  pull = require('pull-stream');


  /* NOTE these two are different: */

  through = require('pull-through');

  async_map = require('pull-stream/throughs/async-map');

  STPS = require('stream-to-pull-stream');

  O = {};

  O.running_in_devtools = console.profile != null;

  PS = require('..');

  ({$, $async} = PS);


  /* Avoid to try to require `v8-profiler` when running this module with `devtool`: */

  simple_recursive_function = function() {
    var f, log;
    log = console.log;
    log(process.versions);
    if (O.running_in_devtools) {
      console.profile('simple_recursive_function');
    }
    f = function(count = 0) {
      log(count);
      if (count < 10) {
        return f(count + 1);
      }
    };
    f();
    if (O.running_in_devtools) {
      return console.profileEnd('simple_recursive_function');
    }
  };

  simple_recursive_function();

}).call(this);

//# sourceMappingURL=benchmarks.js.map
